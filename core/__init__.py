"""Core agent packages.

The public imports used by earlier stages are kept stable through thin
compatibility modules such as ``core.message`` and ``core.todo_store``.
New code should prefer the grouped packages under this directory.
"""

